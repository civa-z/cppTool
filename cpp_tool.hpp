#include <iostream>
#include <vector>

void normalization(std::vector<float> &inputs, double lowerBound, double upperBound);

double correlation_distance(std::vector<double>, std::vector<double>);

double eucliduan_distance(std::vector<double>, std::vector<double>);

